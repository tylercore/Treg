{
  "name": "@tylercore/treg",
  "version": "1.1.4",
  "description": "Treg is a CLI tool that injects an engineering immune system into existing projects.",
  "keywords": [
    "ai-rules",
    "cli",
    "code-quality",
    "formatting",
    "infrastructure",
    "lint",
    "project-setup",
    "testing",
    "treg",
    "typescript"
  ],
  "homepage": "https://github.com/tylercore/treg",
  "bugs": {
    "url": "https://github.com/tylercore/treg/issues"
  },
  "license": "MIT",
  "author": "tylercore <ty.ho1126@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/tylercore/treg.git"
  },
  "bin": {
    "treg": "dist/init-project.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "lint:check": "eslint . --max-warnings 0",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check",
    "type:check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && node scripts/write-dist-package-json.mjs",
    "release": "node scripts/release.mjs",
    "prepare": "husky",
    "prepublishOnly": "pnpm format:check && pnpm lint:check && pnpm type:check && pnpm test && pnpm build"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "mrm-core": "^7.1.22"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "husky": "^9.1.7",
    "jest": "^30.2.0",
    "oxfmt": "^0.43.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
